import React from 'react';
import { JobTaskMap } from '../Jobs/jobTypes';

type Props = {
    taskMap: JobTaskMap;
};

const JobTasks: React.FC<Props> = ({ taskMap }) => {
    return <div>hallo</div>;
};

export default JobTasks;
